To run Yaml filem start and create container:
    docker-compose up
    with detached mode add -detached

To stop Yaml file:
    docker-compose down
    to delete the volumes add -v

To check if the mongoDB is connected
- docker logs <container_name>

if you want to just build any custom images:
 Docker-compose build